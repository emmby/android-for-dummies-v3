<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dummies.android.taskreminder"
    android:versionCode="1"
    android:versionName="1.0">

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <supports-screens
        android:largeScreens="true"
        android:normalScreens="true"
        android:smallScreens="true"
        android:xlargeScreens="true" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name">
        <activity
            android:name=".activity.ReminderLaunchActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.ReminderListActivity" />
        <activity android:name=".activity.ReminderEditActivity" />
        <activity android:name=".activity.ReminderListAndEditorActivity" />
        <activity android:name=".activity.TaskPreferencesActivity" />

        <receiver
            android:name=".receiver.OnBootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
        <receiver android:name=".receiver.OnAlarmReceiver" />

        <provider
            android:name=".provider.ReminderProvider"
            android:authorities="com.dummies.android.taskreminder.provider.ReminderProvider"
            android:exported="false" />
    </application>

</manifest>