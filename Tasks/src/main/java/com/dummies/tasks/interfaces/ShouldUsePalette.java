package com.dummies.tasks.interfaces;

public interface ShouldUsePalette {
    /**
     * Returns true if we should use the Palette library to set the
     * colors in this activity.  False otherwise.
     */
    boolean shouldUsePalette();
}
